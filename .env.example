# PostgreSQL Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/presentation_ai"

# Next Auth Configuration
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET="REPLACE_WITH_YOUR_SECRET"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth Provider (get from: https://console.cloud.google.com/)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ============================================
# OLLAMA CONFIGURATION (PRIMARY AI PROVIDER)
# ============================================
# Ollama is now the main AI provider for this application
# No API keys needed - runs locally on your machine
OLLAMA_BASE_URL="http://localhost:11434"
# Model to use (default: llama3.2 — run `ollama pull llama3.2` first)
OLLAMA_MODEL="llama3.2"

# Optional: Ollama timeout and retry settings
OLLAMA_TIMEOUT_MS="120000"   # 2 minutes default
OLLAMA_MAX_RETRIES="3"       # Number of retries on failure

# ============================================
# EXTERNAL AI PROVIDERS (NOT REQUIRED)
# ============================================
# These are optional and not needed when using Ollama
# Uncomment only if you want to use external AI providers
# OPENAI_API_KEY=""
# TOGETHER_AI_API_KEY=""

# File Upload Service (get from: https://uploadthing.com/)
UPLOADTHING_TOKEN=""

# Optional: Web Search (only needed for outline generation with search)
# Get from: https://tavily.com/
# TAVILY_API_KEY=""

# Optional: Image Search
UNSPLASH_ACCESS_KEY=""
